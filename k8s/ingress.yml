apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: chainlit-chatbot-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: chainlit.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: chainlit-chatbot-service
            port:
              number: 7860
